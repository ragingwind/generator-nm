{
  "name": "<%= moduleName %>",
  "version": "0.0.0",
  "description": "My <%= superb %> module",
  "license": "MIT",
  "repository": "<%= githubUsername %>/<%= moduleName %>",
  "author": {
    "name": "<%= name %>",
    "email": "<%= email %>",
    "url": "<%= humanizedWebsite %>"
  },<% if (cli) { %>
  "bin": "cli.js",<% } %>
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "xo <% if (babel) { %>src/*.js test*.js && npm run babel <% } %>&& ava"<% if (babel) { %>,
    "babel": "babel src --out-dir . --presets=es2015",
    "prepublish": "npm run babel"<% } %>
  },
  "files": [
    "index.js"<% if (cli) { %>,
    "cli.js"<% } %>
  ],
  "keywords": [<% if (cli) { %>
    "cli-app",
    "cli",<% } %>
    ""
  ],
  "dependencies": {<% if (cli) { %>
    "meow": "^3.6.0"
  <% } %>},
  "devDependencies": {
    "ava": "^0.8.0",
    "xo": "^0.12.1"<% if (babel) { %>,
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13"<% } %>
  }
}
